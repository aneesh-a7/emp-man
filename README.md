# Employee Management System

A Django CRUD app for managing employee records — add, view, remove, and search by name.

## Stack
Python, Django

## Run it
pip install django
python manage.py migrate
python manage.py runserver

---
An early Django project
